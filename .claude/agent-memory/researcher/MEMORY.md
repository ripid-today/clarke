# Researcher Agent Memory

## Tool Availability Notes

- WebFetch and WebSearch have been denied in at least one session (2026-02-22). When these tools are unavailable, synthesize from existing library content + Claude's training knowledge. Always disclose the limitation to the user.
- NotebookLM URLs (notebooklm.google.com) require WebFetch to query. If WebFetch is denied, cannot retrieve notebook content directly.
